{
  "name": "server",
  "version": "1.0.0",
  "description": "Server implementation for game 2048",
  "main": "index.js",
  "scripts": {
    "test": "npx jest",
    "test-watch": "npx jest --watchAll",
    "dev": "npx nodemon --watch src --ext ts --exec 'npx tsc -p ./tsconfig.json && node dist/src/index.js'"
  },
  "author": "Marian Durco <majodurco@gmail.com>",
  "license": "ISC",
  "dependencies": {
    "nodemon": "^2.0.6",
    "typescript": "^4.1.3"
  },
  "devDependencies": {
    "@types/jest": "^26.0.19",
    "jest": "^26.6.3",
    "ts-jest": "^26.4.4"
  }
}
